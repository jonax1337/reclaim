# Icons

Replace these placeholders by running, from the project root:

```sh
npm run tauri -- icon path/to/source.png
```

`source.png` should be 1024×1024, square, with transparent background.
The CLI will generate `32x32.png`, `128x128.png`, `128x128@2x.png`, `icon.icns`, `icon.ico` here.
